{
  "name": "30-day-cpp-sprint",
  "version": "1.0.0",
  "description": "30-day project-based C++ curriculum for VEX robotics preparation",
  "scripts": {
    "dev": "npx @11ty/eleventy --serve",
    "build": "npx @11ty/eleventy",
    "build:prod": "npx @11ty/eleventy --quiet"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1"
  },
  "engines": {
    "node": ">=18"
  }
}